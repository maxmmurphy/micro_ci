%h2
  Projects
%hr
%table
  - @projects.each do |project|
    %tr
      %td
        %img{ :src => "/images/#{project.last_build_result}.png" }
      %td
        %a{ :href => "/projects/#{project.name}" }
          = project.name